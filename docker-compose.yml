# HIP-3 Bot Docker Compose
# Trading Mode with Oracle-Driven Entry/Exit

services:
  hip3-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hip3-bot
    restart: unless-stopped
    command: ["--config", "/app/config/mainnet-trading-parallel.toml"]

    # Resource limits (increased for all-market observation)
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

    # Expose dashboard port
    ports:
      - "8080:8080"

    # Volumes for data persistence
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro

    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

    # Environment
    environment:
      - RUST_LOG=info,hip3_bot=debug,hip3_executor=debug,hip3_ws=info,hip3_registry=info
      - RUST_BACKTRACE=1
      - HIP3_TRADING_KEY=${HIP3_TRADING_KEY}

    # Network (outbound only for WebSocket)
    networks:
      - hip3-network

networks:
  hip3-network:
    driver: bridge
